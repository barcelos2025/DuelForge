-- Default settings are defined in the `profiles` table definition as a default JSONB value:
-- '{"sound": true, "music": true, "language": "pt-BR", "vfx_quality": "high"}'::jsonb

-- No specific seed needed for global settings table as it's per-user.
